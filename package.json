{
  "name": "webdata",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "start": "vite preview",
    "build": "next build",
    "build": "echo 'No build step defined' && exit 1"
  },
  "dependencies": {
    "vite": "^4.5.0"
  }
}
